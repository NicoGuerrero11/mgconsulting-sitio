---
import Container from "../ui/Container.astro";
import ValueCard from "./ValueCard.astro";
import { getCollection } from "astro:content";

const valuesRaw = await getCollection("values");
const values = valuesRaw
  .sort((a, b) => (a.data.order ?? 0) - (b.data.order ?? 0))
  .map((v) => ({
    title: v.data.title,
    description: v.data.description,
    icon: v.data.icon,
  }));
---

<section
  class="mx-[calc(50%-50vw)] bg-[var(--bg-white)] md:bg-[var(--bg-grey)] pt-10 md:pt-14 pb-10 mb-0"
>
  <Container class="w-full px-6 md:px-8">
    <h2 class="text-3xl md:text-4xl font-bold text-center">
      Nuestros Valores Fundamentales
    </h2>
    <p class="block md:hidden text-center text-muted text-base mb-8 mt-2">
      Pilares que guían cada acción.
    </p>
    <ValueCard items={values} maxMobile={4} />
    <div class="block md:hidden mt-6 px-4">
      <h3 class="text-xl font-semibold">Nuestra Misión</h3>
      <p class="text-base mt-2 text-muted">
        Nuestra misión es ser el socio estratégico de confianza que impulse a
        las empresas a superar sus desafíos, optimizar su rendimiento y alcanzar
        un crecimiento sostenible en un mundo en constante cambio. Logramos esto
        a través de consultoría experta, soluciones innovadoras y un compromiso
        inquebrantable con el éxito de nuestros clientes.
      </p>
    </div>
  </Container>
</section>
