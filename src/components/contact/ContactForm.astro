---
/* UI del formulario de contacto */
---

<form method="POST" action="/api/contact" class="grid gap-4" novalidate>
  <!-- Honeypot: debe llegar vacío -->
  <input
    type="text"
    name="company_site"
    tabindex="-1"
    autocomplete="off"
    class="hidden"
    aria-hidden="true"
  />
  <!-- Timer anti-bot: server validará que hayan pasado >= 3s -->
  <input type="hidden" name="started_at" value={Date.now().toString()} />

  <div>
    <label for="name" class="block text-sm font-medium text-text">Nombre</label>
    <input
      id="name"
      name="name"
      required
      autocomplete="name"
      placeholder="Tu nombre"
      class="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[var(--color-brand-blue)]"
    />
  </div>

  <div>
    <label for="email" class="block text-sm font-medium text-text">Email</label>
    <input
      id="email"
      name="email"
      type="email"
      required
      autocomplete="email"
      placeholder="tu@empresa.com"
      class="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[var(--color-brand-blue)]"
    />
  </div>

  <div>
    <label for="subject" class="block text-sm font-medium text-text"
      >Asunto</label
    >
    <input
      id="subject"
      name="subject"
      required
      placeholder="Consulta"
      class="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[var(--color-brand-blue)]"
    />
  </div>

  <div>
    <label for="message" class="block text-sm font-medium text-text"
      >Mensaje</label
    >
    <textarea
      id="message"
      name="message"
      required
      rows={5}
      placeholder="Escribe tu mensaje…"
      class="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[var(--color-brand-blue)]"
    ></textarea>
  </div>

  <button type="submit" class="btn-primary w-full md:w-auto">Enviar</button>
</form>
